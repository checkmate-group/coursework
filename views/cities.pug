extends layout

block content
  table
    tr
      th Name
      th CountryCode
      th District
      th Population
    each city in cities 
      tr
          td= city.Name
          td= city.CountryCode
          td= city.District
          td= city.Population